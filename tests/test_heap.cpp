#include "Heap.h"
#include <gtest/gtest.h>
#include <ctime>

TEST(HEAP, BASICUsage) {
	ASSERT_TRUE(0);
	/*
	srand(time(nullptr));
	int a[] = { 2, 4, 8, 3, 6, 22, 9, 1 };
	std::vector<int> v(a, a + 8);
	std::sort(v.begin(), v.end());

	//std::cout << v.size() << std::endl;
	//for (std::vector<int>::iterator i = v.begin(); i < v.end(); ++i)
	//	std::cout << *i << std::endl;

	int* b = new int[8];
	std::copy(v.begin(), v.end(), b);


	int n = sizeof a / sizeof(int);
	Treap tree;
	Treap::pitem treap = tree.build(a, n);
	tree.dfs(treap);
	//printf("\n\n");
	//tree.bfs(treap);
	*/
}